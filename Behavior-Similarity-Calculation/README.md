This part is to calculate the similarity of the behavior category or the overlap of the behavior category detection

Execute the command:
```
python BehaviorSimilarityCalculation.py --labels_path ./5k_HRW_labels
```
